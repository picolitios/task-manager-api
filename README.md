# Task Manager API

API profissional de gerenciamento de tarefas construída com FastAPI e MongoDB Mínimo (Motor).

## Instalação

```bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
```

## Execução

```bash
uvicorn app.main:app --reload
```
